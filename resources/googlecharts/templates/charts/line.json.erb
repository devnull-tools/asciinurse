{
  type: 'google.charts.Line',
  data: google.visualization.arrayToDataTable(<%= @csv.to_json %>),
  options: {
    title: <%= @title.to_json %>
  },
  style: {
    width: <%= @width.to_json %>,
    height: <%= @height.to_json %>
  }
}
